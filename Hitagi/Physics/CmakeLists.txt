add_library(PhysicsManager INTERFACE)
target_include_directories(PhysicsManager INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(PhysicsManager INTERFACE AssetManager)
add_subdirectory(HitagiPhysics)