add_library(physics_manager INTERFACE)
target_include_directories(physics_manager
    INTERFACE
        ${CMAKE_CURRENT_SOURCE_DIR}/include
)
target_link_libraries(physics_manager
    INTERFACE
        asset_manager
)

add_library(hitagi_physics
    src/hitagi_physics.cpp
)
target_link_libraries(hitagi_physics
    PUBLIC 
        physics_manager
    PRIVATE 
        spdlog::spdlog
)
target_include_directories(hitagi_physics
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/include
)
